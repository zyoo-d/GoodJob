package com.goodjob.test;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.Statement;
import java.util.ArrayList;

import com.goodjob.Company;
import com.test.util.DBUtil;

import lombok.Data;

public class TestDAO {
	private Connection conn;
	private Statement stat;
	private PreparedStatement pstat;
	private ResultSet rs;

	public TestDAO() {
		this.conn = DBUtil.open();
	}

	public ArrayList<String> list() {
		try {
			String sql = "select cp_name from tblcompany";

			stat = conn.createStatement();
			rs = stat.executeQuery(sql);

			ArrayList<String> list = new ArrayList<>();
			while (rs.next()) {
				list.add(rs.getString("cp_name"));
			}
			return list;

		} catch (Exception e) {
			System.out.println("TestDAO.list");
			e.printStackTrace();
		}
		return null;
	}

	public int addCode(ArrayList<Company> result) {
		try {
			String sql = "insert into tblCode values (?,?)";

			pstat = conn.prepareStatement(sql);
			for(Company com : result) {
				pstat.setString(1, com.getCode());
				pstat.setString(2, com.getName());
				
			}

			return pstat.executeUpdate();

		} catch (Exception e) {
			System.out.println("TestDAO.addCode");
			e.printStackTrace();
		}
		return 0;
	}
	
}